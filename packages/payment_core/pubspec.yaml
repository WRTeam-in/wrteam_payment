name: payment_core
description: "Core contracts, models, registry and orchestrator for wrteam_payment gateway plugins."
version: 0.0.1
homepage:
publish_to: 'none'

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
