name: payment_stripe
description: "Stripe PaymentSheet gateway plugin for wrteam_payment."
version: 0.0.1
homepage:
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  payment_core:
    path: ../payment_core
  flutter_stripe: ^13.1.0
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
